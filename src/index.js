import currentLocation, { message, name } from "./myModule";

console.log(message);
console.log(name);
console.log(currentLocation);

/**
 * default: no {} can be any name
 */

 /**
  * named: {named1, named2}
  */